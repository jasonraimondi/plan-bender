#!/usr/bin/env node

import { execPath } from "../src/index.mjs";
import { execFileSync } from "node:child_process";

if (!execPath) {
  process.exit(1);
}

try {
  execFileSync(execPath, process.argv.slice(2), { stdio: "inherit" });
} catch (e) {
  process.exit(e.status ?? 1);
}
