id: 10
slug: backend-adapter-interface
name: "Backend adapter interface"
track: intent
status: backlog
priority: medium
points: 2
labels:
  - AFK
assignee: null
blocked_by: [2]
blocking: [11, 12, 13]
branch: null
pr: null
linear_id: null
created: 2026-03-26
updated: 2026-03-26
tdd: true

outcome: |
  A TypeScript interface defining the tracking backend contract, with a factory to instantiate the configured backend.

scope: |
  Interface definition, shared types for state mapping, backend factory. No concrete implementations.

acceptance_criteria:
  - "TrackingBackend interface defines: createProject, createIssue, updateIssue, pullIssue, pullProject methods"
  - "Shared types map between local YAML fields and remote system fields"
  - "createBackend(config) returns the correct backend instance based on config.backend"
  - "Unknown backend value produces a clear error"

steps:
  - "src/backends/interface.ts — TrackingBackend interface with methods: createProject(prd), createIssue(issue), updateIssue(issue), pullIssue(remoteId), pullProject(projectId)"
  - "src/backends/types.ts — shared types: RemoteIssue, RemoteProject, SyncResult (created/updated/unchanged/failed counts)"
  - "src/backends/factory.ts — createBackend(config): switch on config.backend, return instance, throw on unknown backend"

use_cases:
  - UC-4

notes: |
  I/O translation only — no business logic in adapters. The interface is intentionally thin to make adding Jira (or other backends) straightforward later.
