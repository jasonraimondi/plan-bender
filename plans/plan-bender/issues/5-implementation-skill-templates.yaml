id: 5
slug: implementation-skill-templates
name: "Implementation + orchestrator skill templates: review-prd, implement-prd, implement-issue, orchestrator"
track: data
status: done
priority: high
points: 3
labels:
  - AFK
assignee: null
blocked_by: [2, 3]
blocking: [6]
branch: null
pr: null
linear_id: null
created: 2026-03-26
updated: 2026-03-26
tdd: false

outcome: |
  Four .skill.tmpl template files that generate the implementation-phase skills and the master orchestrator, parameterized by config values.

scope: |
  Template files for review-prd, implement-prd, implement-issue, and the orchestrator. More conditional logic than planning templates (tdd/headed flags, backend sync, routing).

acceptance_criteria:
  - "Each template renders to a valid SKILL.md when given default config"
  - "implement-issue template includes backend-specific sync instructions only when backend is not yaml-fs"
  - "review-prd template auto-fix rules reference configured tracks and tdd/headed conventions"
  - "Orchestrator template renders pipeline phases from config, omitting skipped phases"
  - "Orchestrator template shows plan-bender CLI commands (status, validate) in menu"

steps:
  - "templates/planning-review-prd.skill.tmpl — convert existing SKILL.md, parameterize tracks, tdd/headed auto-fix rules, custom field validation"
  - "templates/implement-prd.skill.tmpl — convert existing SKILL.md, parameterize workflow states, backend sync, routing labels, status report format"
  - "templates/implement-issue.skill.tmpl — convert existing SKILL.md, parameterize workflow states, backend sync, branching pattern, tdd/headed skill invocation"
  - "templates/orchestrator.skill.tmpl — menu-driven skill: list active plans with status, offer actions (start new, continue, review, implement, dashboard), render pipeline phases from config minus skipped"

use_cases:
  - UC-2
  - UC-6
  - UC-7

notes: |
  The orchestrator template is new (no existing skill to convert). It reads plan state via plan-bender CLI (plan-bender status) and presents a menu. Source skills for the other three live at /Users/jason/Code/jason/skills/skills/.
