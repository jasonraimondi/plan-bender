id: 4
slug: planning-skill-templates
name: "Planning skill templates: interview, write-prd, prd-to-issues, write-an-issue"
track: data
status: backlog
priority: high
points: 3
labels:
  - AFK
assignee: null
blocked_by: [2, 3]
blocking: [6]
branch: null
pr: null
linear_id: null
created: 2026-03-26
updated: 2026-03-26
tdd: false

outcome: |
  Four .skill.tmpl template files that generate the planning-phase Claude Code skills, parameterized by config values.

scope: |
  Template files for interview-me, write-a-prd, prd-to-issues, write-an-issue. Uses template syntax for tracks, workflow states, step pattern, custom fields, backend conditionals.

acceptance_criteria:
  - "Each template renders to a valid SKILL.md when given default config"
  - "Changing tracks in config produces skills that reference the new track names"
  - "Changing workflow_states in config produces skills with updated state lists"
  - "Custom fields from config appear in write-an-issue template's schema section"
  - "Backend conditionals (e.g., sync instructions) render only when backend is not yaml-fs"
  - "Step pattern from config appears in prd-to-issues and write-an-issue templates"

steps:
  - "templates/planning-interview-me.skill.tmpl — convert existing SKILL.md, parameterize with pipeline phases from config"
  - "templates/planning-write-a-prd.skill.tmpl — convert existing SKILL.md, parameterize PRD schema with plans_dir, dev_command, base_url"
  - "templates/planning-prd-to-issues.skill.tmpl — convert existing SKILL.md, parameterize tracks, workflow states, step pattern, max points, custom fields, tdd/headed rules"
  - "templates/planning-write-an-issue.skill.tmpl — convert existing SKILL.md, parameterize tracks, workflow states, step pattern, max points, custom fields, backend sync instructions"

use_cases:
  - UC-6
  - UC-7

notes: |
  Source skills live at /Users/jason/Code/jason/skills/skills/planning-*. Read each, identify hardcoded values that should become template variables, replace with ${var} and @if/@each blocks.
