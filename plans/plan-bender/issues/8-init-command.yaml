id: 8
slug: init-command
name: "init command: interactive setup + auto-generate + auto-install"
track: intent
status: done
priority: high
points: 2
labels:
  - AFK
assignee: null
blocked_by: [6, 7]
blocking: []
branch: null
pr: null
linear_id: null
created: 2026-03-26
updated: 2026-03-26
tdd: true

outcome: |
  A single plan-bender init command that interactively configures a project and leaves it ready to use.

scope: |
  Interactive prompts, config file generation, auto-run generate-skills + install. First-run experience only.

acceptance_criteria:
  - "plan-bender init prompts for: backend, tracks (default or custom), step pattern, plans directory, max points"
  - "Generates a commented plan-bender.yaml from answers"
  - "Automatically runs generate-skills and install after config creation"
  - "Prints success message with next steps (try /plan-bender)"
  - "Refuses to overwrite existing plan-bender.yaml without confirmation"

steps:
  - "src/cli/commands/init.ts — interactive prompts using enquirer or prompts: backend (yaml-fs/linear), tracks (default set or custom list), step pattern, plans_dir, max_points"
  - "Config generation — serialize answers to plan-bender.yaml with inline comments explaining each field"
  - "Auto-generate — call generate-skills programmatically after config write"
  - "Auto-install — call install programmatically after generation"
  - "Register init command in CLI entry point"

use_cases:
  - UC-1

notes: |
  init is the capstone of v0.1 — it wires together config, generate, and install into one first-run command.
