id: 11
slug: yaml-fs-backend
name: "yaml-fs backend implementation"
track: data
status: backlog
priority: medium
points: 1
labels:
  - AFK
assignee: null
blocked_by: [10]
blocking: [13, 14]
branch: null
pr: null
linear_id: null
created: 2026-03-26
updated: 2026-03-26
tdd: true

outcome: |
  A TrackingBackend implementation that reads and writes local YAML files as the tracking store.

scope: |
  yaml-fs backend only. File I/O for plans_dir, status updates via YAML field writes. No remote API calls.

acceptance_criteria:
  - "Implements TrackingBackend interface"
  - "createIssue writes issue YAML to correct path under plans_dir"
  - "updateIssue updates YAML fields in-place"
  - "pullIssue reads issue YAML and returns structured data"
  - "File paths resolve correctly using config.plans_dir"

steps:
  - "src/backends/yaml-fs.ts — implement TrackingBackend: createProject (mkdir), createIssue (write YAML), updateIssue (read-modify-write YAML), pullIssue (read + parse YAML), pullProject (read all issues)"
  - "Register yaml-fs in backend factory"

use_cases:
  - UC-4

notes: |
  The yaml-fs backend is almost trivially simple — its value is in conforming to the TrackingBackend interface so the sync command works uniformly.
