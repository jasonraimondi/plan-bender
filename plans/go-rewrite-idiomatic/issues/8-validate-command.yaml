id: 8
slug: validate-command
name: "pb validate command"
track: intent
status: backlog
priority: high
points: 1
labels:
  - AFK
assignee: null
blocked_by: [2, 4]
blocking: []
branch: null
pr: null
linear_id: null
created: 2026-03-28
updated: 2026-03-28
tdd: true
headed: false

outcome: |
  First tracer bullet — a working CLI command that loads config, reads a plan, and validates it end-to-end.

scope: |
  ValidatePlan orchestrator + Cobra command wiring + styled error output. Validation logic from issues 3 and 4.

acceptance_criteria:
  - "pb validate <slug> runs full validation pipeline (schema + cross-refs + cycles)"
  - "Styled error output with file paths and field-level messages via Lipgloss"
  - "Exit code 1 on validation failure, 0 on success"

steps:
  - "internal/schema/validate.go — ValidatePlan(slug, config) orchestrating PRD validation, issue validation, cross-refs, cycle detection, returning PlanValidationResult"
  - "internal/cli/validate.go — Cobra command wiring: resolve config, call ValidatePlan, format errors with Lipgloss, set exit code"

use_cases:
  - UC-3

notes: |
  This is the first end-to-end command. Proves config loading + schema + validation + CLI wiring work together.
