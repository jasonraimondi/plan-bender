id: 18
slug: goreleaser-homebrew
name: "goreleaser + Homebrew tap"
track: intent
status: backlog
priority: medium
points: 2
labels:
  - AFK
assignee: null
blocked_by: [1]
blocking: []
branch: null
pr: null
linear_id: null
created: 2026-03-28
updated: 2026-03-28
tdd: false
headed: false

outcome: |
  Automated release pipeline producing cross-platform binaries and Homebrew formula.

scope: |
  goreleaser config, GitHub Actions release workflow, Homebrew tap. No code changes.

acceptance_criteria:
  - "goreleaser produces binaries for linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64"
  - "Both plan-bender and pb binary names produced"
  - "GitHub Actions workflow triggers on tag push"
  - "Homebrew formula in jasonraimondi/homebrew-tap installs correctly"

steps:
  - ".goreleaser.yaml — build config: single cmd/plan-bender entry, 5 platform/arch targets, binary name plan-bender + pb extra files"
  - ".github/workflows/release.yaml — trigger on tag v*, run goreleaser, publish to GitHub Releases"
  - ".goreleaser.yaml — Homebrew tap config pointing to jasonraimondi/homebrew-tap repo"

use_cases:
  - UC-1

notes: |
  goreleaser's extra_files or post hooks can create the pb symlink/copy.
  Homebrew tap is a separate repo (jasonraimondi/homebrew-tap) that goreleaser auto-updates.
