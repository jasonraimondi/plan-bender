id: 17
slug: sync-command
name: "pb sync command"
track: intent
status: backlog
priority: medium
points: 2
labels:
  - AFK
assignee: null
blocked_by: [2, 16]
blocking: []
branch: null
pr: null
linear_id: null
created: 2026-03-28
updated: 2026-03-28
tdd: true
headed: false

outcome: |
  Push/pull sync between local YAML plans and remote backend.

scope: |
  Sync Cobra command with push and pull subcommands. Uses Backend interface from issue 7.

acceptance_criteria:
  - "pb sync push <slug> creates/updates issues in remote backend"
  - "pb sync pull <slug> fetches remote state and updates local YAML"
  - "Push writes linear_id back to issue YAML after creation"
  - "Progress output via Lipgloss"

steps:
  - "internal/cli/sync.go — Cobra command with push/pull subcommands, backend factory creation"
  - "internal/cli/sync.go — push: iterate issues, create if no linear_id else update, write linear_id back to YAML"
  - "internal/cli/sync.go — pull: fetch remote project + issues, compare status/priority, write changed issues"
  - "internal/cli/sync_test.go — mock Backend interface: push creates + writes IDs, pull updates changed fields only"

use_cases:
  - UC-4

notes: |
  Backend factory (issue 7) selects yaml-fs or linear based on config.backend.
  Push is idempotent: existing linear_id means update, missing means create.
