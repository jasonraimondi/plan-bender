id: 16
slug: linear-backend
name: "Linear backend"
track: intent
status: backlog
priority: medium
points: 3
labels:
  - AFK
assignee: null
blocked_by: [7, 15]
blocking: [17]
branch: null
pr: null
linear_id: null
created: 2026-03-28
updated: 2026-03-28
tdd: true
headed: false

outcome: |
  Backend interface implementation for Linear using genqlient client with bidirectional state/priority mapping.

scope: |
  LinearBackend implementing Backend interface. State mapping, priority mapping, CRUD operations, pagination.

acceptance_criteria:
  - "Implements Backend interface using genqlient client"
  - "State mapping: local status -> Linear state name via config.linear.status_map"
  - "Priority mapping: urgent=1, high=2, medium=3, low=4 (bidirectional)"
  - "Reverse mapping on pull (Linear state -> local status)"
  - "Pagination for bulk issue pulls"

steps:
  - "internal/backend/linear.go — LinearBackend struct implementing Backend interface with genqlient client"
  - "internal/backend/linear.go — resolveStateID: look up Linear workflow state by name via genqlient query"
  - "internal/backend/linear.go — mapPriority/reversePriority: bidirectional int<->string conversion"
  - "internal/backend/linear.go — CreateProject, CreateIssue, UpdateIssue with state/priority mapping"
  - "internal/backend/linear.go — PullIssue, PullProject with reverse mapping + pagination"
  - "internal/backend/linear_test.go — table-driven: state mapping, priority mapping, reverse mapping edge cases"

use_cases:
  - UC-4

notes: |
  State mapping uses config.linear.status_map when provided, otherwise falls back to lowercase local status
  as Linear state name. Reverse mapping uses first match with slog.Warn on ambiguous mappings.
