id: 1
slug: project-scaffold
name: "Project scaffold + Cobra skeleton"
track: intent
status: todo
priority: high
points: 1
labels:
  - AFK
assignee: null
blocked_by: []
blocking: [2, 3, 5, 15, 18]
branch: null
pr: null
linear_id: null
created: 2026-03-28
updated: 2026-03-28
tdd: false
headed: false

outcome: |
  Go module builds, Cobra root command runs with version flag and 10 subcommand stubs.

scope: |
  Module init, Cobra wiring, mise config, slog setup, empty internal/ package dirs. No command implementations.

acceptance_criteria:
  - "go build ./cmd/plan-bender produces a binary"
  - "pb --version prints version, pb --help lists all 10 subcommand stubs"
  - "mise install sets up Go 1.24"

steps:
  - "go.mod — init module github.com/jasonraimondi/plan-bender with Go 1.24"
  - "cmd/plan-bender/main.go — Cobra root command with version flag, register 10 subcommand stubs (no-op)"
  - ".mise.toml — pin Go 1.24"
  - "internal/ — create empty package dirs (cli, config, schema, backend, template, linear)"
  - "cmd/plan-bender/main.go — configure slog default structured logger in root command PersistentPreRun"

use_cases:
  - UC-1

notes: |
  Subcommand stubs print "not implemented" and exit 0. Real implementations come in later issues.
