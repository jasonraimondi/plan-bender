id: 9
slug: template-fixes-dead-code-cleanup
name: "Fix template loader, type context, and remove dead code"
track: intent
status: done
priority: medium
points: 2
labels:
  - AFK
assignee: null
blocked_by: [3]
blocking: [10]
branch: main
pr: null
linear_id: RAL-39
linear_url: "https://linear.app/jboxralph/issue/RAL-39/fix-template-loader-type-context-and-remove-dead-code"
created: 2026-03-27
updated: 2026-03-27
tdd: false
headed: false

outcome: |
  Template context is a typed interface. Missing bundled templates directory throws instead of silently failing. Dead code and developer-thought-process comments removed.

scope: |
  src/templates/ module and dead code cleanup across codebase. Does not change template rendering behavior or add new template features.

acceptance_criteria:
  - "buildTemplateContext returns typed TemplateContext interface (not Record<string, unknown>)"
  - "Dead 'linear' field removed from template context (line 56 of context.ts)"
  - "Missing bundled templates directory throws error (not silently ignored)"
  - "Dead code in src/schemas/cycles.ts lines 4-21 deleted"
  - "Developer-thought-process comments removed from source files"

steps:
  - "src/templates/context.ts — define TemplateContext interface with typed fields (config values, pipeline phases, backend sync flag, etc.); change buildTemplateContext return type from Record<string, unknown> to TemplateContext"
  - "src/templates/context.ts — remove dead linear field (line 56); keep has_backend_sync field which templates use via @if"
  - "src/templates/loader.ts — throw Error('Bundled templates directory not found: <path>') if neither BUNDLED_DIR nor fallback path exists (instead of silent empty result)"
  - "src/schemas/cycles.ts — delete dead code lines 4-21 (unused initial adjacency map setup; correct implementation starts after)"
  - "src/**/*.ts — search for and remove developer-thought-process comments (e.g. '// This is the correct approach because...', '// We do this instead of...')"

use_cases: []

notes: |
  The TemplateContext interface should match what the .skill.tmpl files actually reference via ${var} and @if conditionals. Fields include: project name, tracks, workflow states, step pattern, custom fields, pipeline phases, backend sync flag, and plans directory.
