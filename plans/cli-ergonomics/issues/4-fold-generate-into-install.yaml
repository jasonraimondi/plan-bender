id: 4
slug: fold-generate-into-install
name: "Fold generate-skills into install"
track: intent
status: todo
priority: medium
points: 2
labels:
  - AFK
assignee: null
blocked_by: []
blocking: [6, 8]
branch: null
pr: null
linear_id: null
created: 2026-03-28
updated: 2026-03-28
tdd: true
headed: false

outcome: |
  pb install regenerates skills automatically before symlinking. generate-skills is removed as a top-level command.

scope: |
  Refactor generate.go to export a function, update install.go to call it, remove command from main.go, update init.go to rely on install.

acceptance_criteria:
  - "pb install on a fresh project generates skills and symlinks them in one command"
  - "generate-skills is not listed in pb --help"
  - "pb init still works end-to-end (config + install)"
  - "Existing generate and install tests pass"

steps:
  - "generate.go — extract logic into exported GenerateSkills(root string, cfg config.Config, out io.Writer) (int, error) returning count"
  - "generate.go — make NewGenerateSkillsCmd a thin wrapper calling GenerateSkills (keep for now as unexported, or remove)"
  - "install.go:NewInstallCmd — call GenerateSkills(root, cfg, cmd.OutOrStdout()) before symlinking, remove os.Stat pre-check"
  - "cmd/plan-bender/main.go — remove cli.NewGenerateSkillsCmd() from root.AddCommand"
  - "init.go:NewInitCmd — remove manual genCmd invocation since install now generates internally"
  - "install_test.go — remove manual generate step before install, verify install generates + symlinks"
  - "generate_test.go — update tests to call GenerateSkills function directly instead of command"

use_cases:
  - UC-4

notes: null
