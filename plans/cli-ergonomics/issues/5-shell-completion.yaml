id: 5
slug: shell-completion
name: "Add shell completion with slug auto-complete"
track: experience
status: todo
priority: medium
points: 2
labels:
  - AFK
assignee: null
blocked_by: []
blocking: []
branch: null
pr: null
linear_id: null
created: 2026-03-28
updated: 2026-03-28
tdd: true
headed: false

outcome: |
  pb completion bash/zsh/fish outputs valid shell scripts. Slug arguments auto-complete from plan directory names.

scope: |
  New completion.go file, slug completion function, registration on existing commands. No changes to command behavior.

acceptance_criteria:
  - "pb completion bash outputs valid bash completion script"
  - "pb completion zsh outputs valid zsh completion script"
  - "pb completion fish outputs valid fish completion script"
  - "Tab-completing after pb status lists plan slugs from plans dir"
  - "Same slug completion works on validate, graph, archive, write-prd, write-issue, sync push, sync pull"

steps:
  - "completion.go — create NewCompletionCmd() with bash/zsh/fish subcommands using Cobra's GenBashCompletionV2, GenZshCompletion, GenFishCompletion"
  - "completion.go:slugCompletionFunc — implement cobra.ValidArgsFunction that loads config, reads plan dir entries, returns dir names as completions"
  - "cmd/plan-bender/main.go — register completion command via root.AddCommand"
  - "cmd/plan-bender/main.go — set ValidArgsFunction on validate, graph, archive, status commands"
  - "sync.go — set ValidArgsFunction on push and pull subcommands"
  - "completion_test.go — test slugCompletionFunc returns correct directory names from a temp plans dir"

use_cases:
  - UC-5

notes: |
  write-prd and write-issue also take slugs but the completion is the same function. Register it via ValidArgsFunction on those commands too after issues #1 and #2 land — or register it now since the function is independent.
