version: 2

builds:
  - id: plan-bender
    main: ./cmd/plan-bender
    binary: plan-bender
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    hooks:
      post:
        - cmd: cp {{ .Path }} {{ dir .Path }}/pb{{ if eq .Os "windows" }}.exe{{ end }}
          output: true

  - id: plan-bender-agent
    main: ./cmd/plan-bender-agent
    binary: plan-bender-agent
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    hooks:
      post:
        - cmd: cp {{ .Path }} {{ dir .Path }}/pba{{ if eq .Os "windows" }}.exe{{ end }}
          output: true

archives:
  - id: plan-bender
    ids: [plan-bender]
    formats: [tar.gz, zip]
    name_template: "{{ .Binary }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - src: "dist/plan-bender-agent_{{ .Os }}_{{ .Arch }}*/plan-bender-agent{{ if eq .Os \"windows\" }}.exe{{ end }}"
        dst: .
        strip_parent: true
  - id: plan-bender-agent
    ids: [plan-bender-agent]
    formats: [tar.gz, zip]
    name_template: "{{ .Binary }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

# brews:
#   - ids: [plan-bender]
#     repository:
#       owner: jasonraimondi
#       name: homebrew-tap
#     homepage: https://github.com/jasonraimondi/plan-bender
#     description: Plan management CLI tool
#     install: |
#       bin.install "plan-bender"
#       bin.install "plan-bender-agent"
#       bin.install_symlink bin/"plan-bender" => "pb"
#       bin.install_symlink bin/"plan-bender-agent" => "pba"
#     test: |
#       system "#{bin}/plan-bender", "--version"
